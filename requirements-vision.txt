# Local face / race analysis for scraper.mugshot_ethnicity
# Fully offline after first model download (weights under ~/.deepface/).
#
#   python -m pip install --user -r requirements-vision.txt
#
# Pin numpy below 2.3: newer wheels (2.4+) often break prebuilt
# tensorflow/keras/pandas binaries ("numpy.dtype size changed").
numpy>=1.26.0,<2.3

# Primary stack — DeepFace (https://github.com/serengil/deepface)
# Open-source face attribute pipeline with built-in race model
# (White, Black, Asian, Indian, Middle Eastern, Latino Hispanic).
deepface>=0.0.93
tensorflow>=2.15.0
tf-keras>=2.15.0
opencv-python-headless>=4.8.0
pillow>=10.0.0

# Optional fallback (not required if deepface works):
# torch>=2.0.0
# transformers>=4.36.0
